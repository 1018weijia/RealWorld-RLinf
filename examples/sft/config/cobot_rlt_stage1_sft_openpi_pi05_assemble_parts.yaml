defaults:
  - model/pi0_5_rlinf_cobot@actor.model
  - hybrid_engines/fsdp@actor.fsdp_config
  - _self_
  # Apply RLT flags after _self_ so they are not wiped by task openpi: keys.
  - rlt/stage1_rlt_openpi_pi05_common
  # Hydra requires overrides at the end of the defaults list.
  - override hydra/job_logging: stdout

hydra:
  run:
    dir: .
  output_subdir: null
  searchpath:
    - file://${oc.env:EMBODIED_PATH}/config/

cluster:
  num_nodes: 1
  component_placement:
    actor,env,rollout: ${oc.env:COBOT_GPU_ID,0}

runner:
  task_type: sft
  logger:
    log_path: "../results"
    project_name: RLinf_rlt_stage1
    experiment_name: "cobot_rlt_stage1_assemble_parts"
    logger_backends: ["wandb", "tensorboard"]

  max_epochs: -1
  max_steps: 30000
  val_check_interval: -1
  save_interval: 5000

data:
  train_data_paths: "cobot_magic/assemble_parts"
  num_workers: 8
  # Proposal: keep failure episodes for RL-token coverage.
  include_failure_episodes: true

actor:
  group_name: "ActorGroup"
  training_backend: "fsdp"
  micro_batch_size: 64
  global_batch_size: 64
  seed: 0

  model:
    precision: bf16
    # Start from already-trained torch pi0.5 Cobot assemble_parts SFT weights.
    model_path: "/data/gxy/realworldRL/RLinf/logs/20260823-051812-cobot_sft_openpi_rlinf_pi05_assemble_parts-gpu3/cobot_pi05_assemble_parts_sft/checkpoints/global_step_30000/actor/model_state_dict"
    num_action_chunks: 50
    action_dim: 14
    add_value_head: False
    openpi:
      task: sft
      config_name: "pi05_cobot_magic"
      assets_dir: /data/gxy/realworldRL/checkpoints/assets
      asset_id: "cobot_magic/assemble_parts"
      num_images_in_input: 3
      discrete_state_input: True
      max_token_len: 200
      model_action_dim: 32
    openpi_data:
      default_prompt: "assemble parts"
      norm_stats_path: /data/gxy/realworldRL/checkpoints/assets/cobot_magic/assemble_parts/norm_stats.json

  optim:
    lr: 1.0e-5
    adam_beta1: 0.9
    adam_beta2: 0.95
    adam_eps: 1.0e-08
    weight_decay: 1e-10
    clip_grad: 1.0
    total_training_steps: 30000
    lr_warmup_steps: 1000
    lr_scheduler: "openpi_cosine"
    num_cycles: 0.5
    min_lr: 2.5e-6

  fsdp_config:
    strategy: "fsdp"
    sharding_strategy: "no_shard"
    use_orig_params: True
    gradient_checkpointing: True
    gradient_checkpointing_use_reentrant: False
    mixed_precision:
      param_dtype: bf16
      reduce_dtype: fp32
      buffer_dtype: fp32

reward:
  use_reward_model: False

critic:
  use_critic_model: False
